---
"@raisins/schema": minor
---

Add optional `hidden?: boolean` flag to `CustomElement`. Editors can use it to surface custom elements that have no visible canvas representation (e.g. modals) via dedicated affordances such as a "hidden components" flyout.
